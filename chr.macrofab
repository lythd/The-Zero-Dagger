#:name:# 
#:path:# 

// If we used CNROM, we'd store each CHR file at a different offset.
// Instead we used mapper30 so need to pbz it.
// Note that you don't have to use "path"; you could use "name" to derive a filename instead.

ct U chr_#name#_index = _index

// Define the tileset (commonly called CHR)
data /pbz
    [] chr_#name#
         file(pbz, #"path"#)
